# 123 NIGHT CLUB 官網・部署說明

**版本 v6.4.6（2026-08-24 定版）｜熱灣行銷**

## 一、這包裡有什麼

| 檔案 | 說明 |
|---|---|
| `index.html` | 首頁（進站入口、QR 過場都在這） |
| `events.html` | 當月活動 |
| `girls.html` | 123 Girls |
| `menu.html` | 消費方式 |
| `videos/intro.mp4` | 掃碼過場影片（主用） |
| `videos/intro.webm` | 過場影片備援格式 |
| `videos/intro_logo.webp` | 過場字標圖 |

網頁的圖片、字卡、字體全部內嵌在 HTML 裡，**不需要再上傳其他素材**。

## 二、上傳方式（給主機商／工程師）

1. 整包解壓縮，**照原本的資料夾結構**全部上傳到網站根目錄——四個 HTML 在根目錄、`videos/` 資料夾原樣放旁邊，一個都不能少。
2. 入口網頁是 `index.html`（一般主機預設就會用它當首頁）。
3. 不需要資料庫、不需要後端程式，任何靜態網頁空間（含最便宜的虛擬主機）都能跑。

## 三、QR code 規則（重要）

- 店內 QR code 的內容必須是：**網址加上 `?qr=1`**，例如 `https://你的網域/?qr=1`
- 只有掃碼進站的客人會看到過場動畫；直接輸入網址進站不會播。
- 每支手機同一場（瀏覽器沒關）只播一次；想重看可用 `https://你的網域/?intro=1` 測試。
- **QR code 圖檔等網域確定後由 Claude 產出**，不要自己先做。

## 四、上線後測試清單（手機掃碼跑一次）

1. 掃碼進站 → 先出現 18 歲年齡門 → 點「是」→ 過場動畫（影片＋字標）→ 白粉閃 → 落到首頁。
2. 桌機開 `?qr=1` → 點「是」後是 CSS 版過場（不是影片），正常。
3. 同一支手機再掃一次 → 不再播過場、也不再問 18 歲，直接進站，正常。
4. 右上角語言可切 中／日／EN；日文手機自動變日文。
5. 首頁「本日主打星」照常輪播；「今晚上班」班表字樣尚未出現，正常（見下）。

## 五、還沒接上的兩件事（不影響上線）

| 事項 | 差什麼 | 誰動 |
|---|---|---|
| 班表（看誰今晚上班） | 等店長的 Google 試算表「發布到網路」CSV 網址；提供後 Claude 烘入出新版網站包，重新上傳即生效 | 店長→Claude |
| QR code 圖檔 | 等網域定案 | Claude |

## 六、日後改內容怎麼辦

活動檔期、文案、翻譯、班表網址等所有修改，**一律由 Claude 改版後交付新的網站包**，整包重新上傳覆蓋即可——不要直接手改 HTML（會被下一版蓋掉，也容易改壞內嵌資料）。
